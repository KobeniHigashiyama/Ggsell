<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <!--
            Проверка гонок требует поднятого стека и настоящей сети, поэтому
            вынесена в отдельный набор: `php artisan test` должен проходить и
            без docker compose up.
        -->
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing" force="true"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file" force="true"/>
        <env name="BCRYPT_ROUNDS" value="4" force="true"/>
        <env name="BROADCAST_CONNECTION" value="null" force="true"/>

        <!--
            Тесты идут против настоящего PostgreSQL, а не SQLite в памяти.
            Всё, что здесь проверяется — блокировки строк, SKIP LOCKED,
            частичные индексы, поведение при нарушении ограничений — в SQLite
            либо отсутствует, либо ведёт себя иначе. Тест на другой СУБД
            проверял бы не ту систему, которую мы поставляем.
        -->
        <env name="DB_CONNECTION" value="pgsql" force="true"/>
        <env name="DB_HOST" value="postgres" force="true"/>
        <env name="DB_PORT" value="5432" force="true"/>
        <env name="DB_DATABASE" value="ggsell_test" force="true"/>
        <env name="DB_USERNAME" value="ggsell" force="true"/>
        <env name="DB_PASSWORD" value="secret" force="true"/>

        <env name="CACHE_STORE" value="array" force="true"/>
        <env name="MAIL_MAILER" value="array" force="true"/>
        <env name="QUEUE_CONNECTION" value="sync" force="true"/>
        <env name="SESSION_DRIVER" value="array" force="true"/>

        <!-- Test stubs remain deterministic; each scenario selects failures explicitly. -->
        <env name="STUB_A_ERROR_RATE" value="0" force="true"/>
        <env name="STUB_A_TIMEOUT_RATE" value="0" force="true"/>
        <env name="STUB_A_HANG_SECONDS" value="0" force="true"/>
        <env name="STUB_B_ERROR_RATE" value="0" force="true"/>
        <env name="STUB_B_TIMEOUT_RATE" value="0" force="true"/>
        <env name="STUB_B_HANG_SECONDS" value="0" force="true"/>
        <env name="SUPPLIER_MAX_ATTEMPTS" value="2" force="true"/>
        <env name="SUPPLIER_BACKOFF_BASE_MS" value="1" force="true"/>
        <env name="SUPPLIER_BACKOFF_MAX_MS" value="2" force="true"/>
    </php>
</phpunit>
